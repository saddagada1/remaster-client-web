import type { NextPage } from "next";
import Redirect from "../../components/Helpers/Redirect";

const Track: NextPage = ({}) => {
  return <Redirect path="/" title="Track" />;
};

export default Track;
